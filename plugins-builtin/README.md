# Built-in Skill Plugins

Official built-in Skills compiled directly into the engine for maximum performance.

These Go-native Skills bypass the sandbox (no `unsafe` or `os/exec` allowed) and run
as part of the engine process.
