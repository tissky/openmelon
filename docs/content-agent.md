# Content Agent

OpenMelon is a specialized content agent.

It is optimized for creative workflows rather than arbitrary task execution. The agent manages:

- creative brief interpretation,
- content angle selection,
- copy and script work,
- visual prompt concretization,
- multimodal generation,
- review and iteration,
- project memory updates.

This specialization lets OpenMelon build deeper production structure than a generic assistant.
