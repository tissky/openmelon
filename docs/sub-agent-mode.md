# Sub-Agent Mode

OpenMelon can run as a sub-agent inside a broader agent system.

In sub-agent mode, the host agent provides:

- project context,
- requested workflow stage,
- available assets,
- constraints,
- expected output type.

OpenMelon returns structured artifacts, labels, provenance, and review results.
