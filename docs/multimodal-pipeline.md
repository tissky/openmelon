# Multimodal Pipeline

OpenMelon coordinates artifacts across modalities:

- text,
- image,
- audio,
- video.

A multimodal production pipeline should keep links between artifacts. For example, an image prompt may come from a script beat, and a video shot list may depend on image references.

Every artifact should carry provenance.
