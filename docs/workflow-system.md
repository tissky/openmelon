# Workflow System

A workflow is a staged content production process.

Typical stages:

```text
intent_planning -> angle_selection -> copywriting -> visual_concretization -> generation -> review -> packaging
```

Each stage can select and compile Skill-Plus packages. Each output becomes an artifact with provenance.
